{
  "name": "squadify",
  "version": "1.0.0",
  "scripts": {
    "dev": "tailwindcss --input=app.css --output=../squadify/static/app.css --watch",
    "prod": "tailwindcss --input=app.css --output=../squadify/static/app.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/forms": "0.4.x",
    "tailwindcss": "3.0.x"
  }
}
