{
  "name": "squadify",
  "version": "1.0.0",
  "scripts": {
    "dev": "tailwindcss --input=app.css --output=../squadify/static/app.css --postcss --watch",
    "prod": "NODE_ENV=production tailwindcss --input=app.css --output=../squadify/static/app.css --postcss --minify"
  },
  "dependencies": {
    "@tailwindcss/forms": "0.3.x",
    "autoprefixer": "10.4.x",
    "postcss": "8.4.x",
    "postcss-cli": "9.0.x",
    "tailwindcss": "2.2.x"
  }
}
